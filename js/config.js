// js/config.js

// --- SINGLE SOURCE OF TRUTH FOR BACKEND URL ---
// Since the frontend and backend are now on the same domain,
// we can use a relative path. An empty string means the API calls
// will go to the same host that served the webpage.
const BACKEND_API_BASE_URL = '';